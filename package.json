{
  "name": "tree-sitter-cisco",
  "version": "0.1.0",
  "description": "Cisco configuration parser for tree-sitter",
  "main": "index.js",
  "types": "bindings/node/index.d.ts",
  "scripts": {
    "install": "node-gyp-build",
    "prebuildify": "prebuildify --napi --strip",
    "test": "npm run test:operational && npm run test:config",
    "test:operational": "node scripts/safe-run.js tree-sitter test",
    "test:config": "cd cisco-config && node ../scripts/safe-run.js tree-sitter test",
    "test:update": "node scripts/safe-run.js tree-sitter test -u && cd cisco-config && node ../scripts/safe-run.js tree-sitter test -u",
    "build": "node scripts/safe-run.js tree-sitter generate",
    "build:config": "cd cisco-config && node ../scripts/safe-run.js tree-sitter generate",
    "sync-metadata": "node scripts/sync-metadata.js",
    "debug-server": "node scripts/trace_analyzer.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Alcarin/Tree-Sitter-Cisco.git"
  },
  "keywords": [
    "tree-sitter",
    "cisco",
    "ios",
    "nxos",
    "asa"
  ],
  "author": "Francesco Rubeo",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "node-addon-api": "^8.0.0",
    "node-gyp-build": "^4.8.0"
  },
  "peerDependencies": {
    "tree-sitter": "^0.23.0"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "nan": "^2.20.0",
    "prebuildify": "^6.0.0",
    "tree-sitter-cli": "^0.26.8"
  },
  "bugs": {
    "url": "https://github.com/Alcarin/Tree-Sitter-Cisco/issues"
  },
  "homepage": "https://github.com/Alcarin/Tree-Sitter-Cisco#readme"
}
